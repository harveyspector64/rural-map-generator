// src/config/GeneratorConfig.js

export const TILE_SIZE = 32; // Base tile size, will be adjusted for screen size
export const MAP_WIDTH = 50;
export const MAP_HEIGHT = 50;

export const GENERATION_PARAMS = {
  TREE_CHANCE: 0.1,
  BUSHY_GRASS_CHANCE: 0.05,
  // Add more generation parameters as needed
};
